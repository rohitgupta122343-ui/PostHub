

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        
        <div class="flex w-full justify-end gap-5">
             <a class="bg-blue-500 rounded-md px-3 py-2" href="/profile/upload">My Account</a>
            <a class="bg-red-500 rounded-md px-3 py-2" href="/logout">logout</a>
           
        </div>
       <div class="flex gap-3">
        <img class="w-10 h-10 rounded-md object-cover" src="/image/upload/<%= user.profilePic %>" >
        <h3 class="text-2xl"><span>hello ,</span> <%= user.name %> </h3>
       </div>
        
        <form method="post" action="/post">
            <textarea class="w-1/3 resize-none outline-none bg-transparent border-2 border-zinc-800 px-3 py-2 mt-5 block" name="content" placeholder="What Your mind?"></textarea>
            <input class="bg-blue-500 px-3 py-2 rounded-md mt-2" type="submit" value="Create a post">
        </form>


        <div class="post mt-15">
            <div class="post-container">
              
                <% user.post.reverse().forEach(function(post){ %>

                      <div class="post p-3 w-1/3 border-2 outline-none bg-transparent border-zinc-800 mb-5 rounded-md">
                    <h3 class="text-blue-500">@<%= user.name %></h3>
                    <p class="text-sm tracking-tighter"> <%=post.content%></p>
                    <small>1 like</small>
                    <div class="mt-4 flex gap-80">
                        <a class="text-blue-500" href="">like</a>
                        <a class="text-zinc-500" href="/edit/<%=post.id%>">Edit</a>
                    </div>
                </div>

             <%  }) %>
                
            </div>
        </div>



    </div>


</body>
</html>